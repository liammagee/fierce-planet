<%= form_for(@feedback, :remote => true) do |f| %>
  <% if @feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
      <% @feedback.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div>Please leave a comment:</div>
  <div class="field">
    <%= f.text_area :comments, :size => "60x10" %>
  </div>
  <!--<div class="actions">-->
    <!--<%= f.submit "Submit Feedback" %>-->
  <!--</div>-->
<% end %>
<div>
    For further information, please contact <a href="mailto:liam.magee@gmail.com">Liam Magee</a>.
</div>
