
       <div id="info">
          <div id="global-info">
            <div id="user-links">
                <% if user_signed_in? %>
                    <div id="profile" style="display:none"> <span id="profile-class-display">0</span> </div>
                    <span id="welcome-link">
                      <%= link_to("Welcome #{current_user.profile.nickname} (#{current_user.profile.profile_class})", edit_profile_path(current_user.profile), :remote => true) %> |
                    </span>
                      <a target="_blank" href="http://blog.fierce-planet.com/" id="blog">Blog</a> |
                    <!--<%= link_to('Contact', new_feedback_path, :remote => true) %> |-->
                    <a href="#" id="credits">Credits</a> |
                    <%= link_to('Logout', destroy_user_session_path) %>
                <% else %>
                    <%= link_to('Login', new_user_session_path) %>
                <% end %>
            </div>
          </div>
        </div>